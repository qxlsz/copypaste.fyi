{
  "name": "copypaste.fyi",
  "install": "./.cursor/install.sh",
  "terminals": [
    {
      "name": "backend",
      "command": "ROCKET_ADDRESS=127.0.0.1 ROCKET_PORT=8000 COPYPASTE_FORCE_MEMORY=true COPYPASTE_REQUIRE_WRITE_AUTH=false cargo run --bin copypaste -- serve",
      "description": "Rocket backend API on http://127.0.0.1:8000 (in-memory store, open public writes)."
    },
    {
      "name": "frontend",
      "command": "cd frontend && npm run dev -- --host 127.0.0.1 --port 5173",
      "description": "Vite dev server on https://127.0.0.1:5173 (mkcert TLS); proxies /api and /raw to the backend."
    }
  ],
  "ports": [
    { "name": "backend", "port": 8000 },
    { "name": "frontend", "port": 5173 }
  ]
}

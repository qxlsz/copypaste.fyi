# Include from ~/.config/ghostty/config:
#   config-file = ?copypaste
#
# Select text → it lands on the OS clipboard.
# Then `copypaste clip` (or the macOS Service) sends it to your server
# and replaces the clipboard with the share URL.
copy_on_select = true
